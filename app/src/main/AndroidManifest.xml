<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.arbind.carparkingapp">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CarParkingApp">
        <activity android:name=".test"></activity>
        <activity android:name=".deatil" />
        <activity android:name=".booking" />
        <activity android:name=".parking_status_page" />
        <activity android:name=".support_contact" />
        <activity android:name=".location_area" />
        <activity android:name=".parking_list_page" />
        <activity android:name=".parking_pages" />
        <activity android:name=".parking_manual" />
        <activity android:name=".userdetail" />
        <activity android:name=".bluetooth_page" />
        <activity android:name=".forgetpass" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".LoginandregisterActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>